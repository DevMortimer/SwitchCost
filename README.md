# SwitchCost

Track time spent per application on macOS.

## Usage

```bash
./build.sh           # Build binary
./dist/SwitchCost    # Run (Ctrl+C to exit)
./dist/SwitchCost status  # Show stats
```

## Events

Stored in `/tmp/SwitchCost/events.ndjson`.
